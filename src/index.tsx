import ReactDOM from 'react-dom';

const App = () => {
  return <div>
    <textarea></textarea>
    <div>
      <button></button>
    </div>
  </div>
};

ReactDOM.render(<App />, document.querySelector('#root'));
